type NavigationProps = {
  userRole?: string;   // optional, can be "admin", "user", etc.
};

export default function Navigation({ userRole }: NavigationProps) {
  // you can use userRole inside if needed
  // ...
}
